const express = require('express');
const app = express();
const port = process.env.PORT || 3000;

app.get('/', (req, res) => {
  res.send('Hello, Autobot!');
});

app.listen(port, () => {
  console.log(`Autobot app is running on port ${port}`);
});
